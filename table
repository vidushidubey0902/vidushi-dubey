#include <stdio.h>
#include <conio.h>
void main()
{
int a,b,c;
clrscr();
printf("enter a no.");
scanf("%d",&a);

printf("table of %d is",a);
for(b=1;b>=10;b++)
{
c=a*b;
printf("\n%d * %d = %d",a ,b,c);
}
getch();
}
