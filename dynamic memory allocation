#include <stdio.h>
#include <conio.h>
#include <alloc.h>
void main()
{
int i,*ptr,n;
printf("enter no. of elements to be entered");
scanf("%d",&n);
ptr=(int*)malloc(n * sizeof(int));
for(i=0;i<n;i++)
{
printf("enter element"6);
scanf("%d",*ptr=i);
getch();
}
free(ptr);
}
